cloudBox World Format - by tyteen4a03 with input from fragmer

File Extension: .cw (Classic World. We are so creative!)
File Format: zlib'd NBT:

CLASSIC_WORLD: Compound[
    LEVEL_VERSION: Byte // Version of level file.
    LevelName: String()
    LevelGUID: ByteArray() // Random bytes. This must stay intact in between loading/saving worlds. In Python uuid.uuid4() can be used.
    // Coordinates are all shorts, ranging from 0 to 2047. At runtime 1 is added to support maximum 2048^3 map size.
    X: Short() // Length of level.
    Y: Short() // Width of level.
    Z: Short() // Height of level.
    CreatedBy: [ // The creator of this map.
        Service: String() // Minecraft or ClassiCube.
        Username: String()
    ]
    MapGeneratorUsed: Compound[ // How this map was created.
        Software: String() // cloudBox, obviously
        MapGeneratorName: String()
    ]
    TimeCreated: Int() // Time of level creation. Unix timestamp.
    LastAccessed: Int() // Last time it was accessed.
    LastModified: Int() // Last time it was modified.
    Spawn: Compound[ // Spawn point.
        X: Short()
        Y: Short()
        Z: Short()
        H: Byte() // Heading
        P: Byte() // Pitch
    ]
    // Byte array of blocks. Index is calculated using this formula: (z * Length + y) * Width + x
    BlockArray: ByteArray()

    Metadata: Compound[
        // Softwares can add their metadata here.
        softwareName: Compound[
            pluginName: Compound[
                // Plugins decide their own format here.
            ]
        ]
    ]
    BlockMetadata: List(Compound)[
        // Each item in the list represents one block. Plugin authors wishing to add metadata to the same block will
        // use the same entry.
        [
            ID: Int() // Index of block.
            Content: Compound[
                softwareName: Compound[
                    pluginName: Compound[
                        // Plugins decide their own format here.
                    ]
                ]
            ]
        ]
    ]
]

Notes:
1. Reserved Metadata softwareNames are: CPE